import type { NextPage } from 'next';
import { Header } from '@amps-games/core-ui';

const Home: NextPage = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Home;
